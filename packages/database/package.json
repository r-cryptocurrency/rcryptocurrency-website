{
  "name": "@rcryptocurrency/database",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "set -a; . ../../.env; set +a; prisma generate",
    "db:push": "set -a; . ../../.env; set +a; prisma db push",
    "db:reset": "set -a; . ../../.env; set +a; prisma db push --force-reset"
  },
  "dependencies": {
    "@prisma/client": "^5.21.1"
  },
  "devDependencies": {
    "prisma": "^5.21.1",
    "@rcryptocurrency/tsconfig": "workspace:*",
    "@types/node": "^18.0.0",
    "typescript": "^5.4.5"
  }
}