{
  "name": "@rcryptocurrency/database",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "set -a; . ../../.env; set +a; prisma generate",
    "db:push": "set -a; . ../../.env; set +a; prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^4.14.0"
  },
  "devDependencies": {
    "prisma": "^4.14.0",
    "@ccmoons/tsconfig": "workspace:*",
    "typescript": "^5.1.0"
  }
}