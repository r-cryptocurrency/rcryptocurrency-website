{
  "name": "@rcryptocurrency/ledger",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/src/index.js",
    "lint": "eslint .",
    "ingest-csv": "ts-node src/ingest-csv.ts",
    "calc-earned-moons": "ts-node scripts/calc-earned-moons.ts",
    "backfill-burns": "ts-node scripts/backfill-burns.ts",
    "backfill-swaps": "ts-node scripts/backfill-swaps.ts",
    "refresh-balances": "ts-node scripts/refresh-balances.ts",
    "health-check": "ts-node scripts/health-check.ts",
    "monitor": "ts-node -r tsconfig-paths/register scripts/monitor-moons.ts",
    "seed-labels": "ts-node scripts/seed-labels.ts",
    "label": "ts-node scripts/label-address.ts",
    "cleanup": "ts-node scripts/cleanup-duplicates.ts"
  },
  "dependencies": {
    "@rcryptocurrency/chain-data": "workspace:*",
    "@rcryptocurrency/database": "workspace:*",
    "viem": "^1.0.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@rcryptocurrency/tsconfig": "workspace:*",
    "@rcryptocurrency/eslint-config": "workspace:*",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.0",
    "@types/node": "^18.0.0"
  }
}